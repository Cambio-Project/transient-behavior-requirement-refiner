import { LogicOperatorPipe } from './logic-operator.pipe';

describe('LogicOperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new LogicOperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
