import { ValidationResponse } from './validation-response';

describe('ValidationResponse', () => {
  it('should create an instance', () => {
    expect(new ValidationResponse()).toBeTruthy();
  });
});
